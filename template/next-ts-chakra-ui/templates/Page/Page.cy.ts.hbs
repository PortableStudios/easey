import { checkA11y } from '{{#each upDir}}../{{/each}}support/utils';

describe('{{name}}', () => {
  it('loads successfully', () => {
    cy.visit('/{{filename}}');
  });

  it('has no detectable a11y violations on load', () => {
    cy.visit('/{{filename}}');
    checkA11y();
  });
});
