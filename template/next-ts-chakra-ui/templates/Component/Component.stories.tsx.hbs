import React from 'react';

import {{name}} from './{{name}}';

export default {
  title: '{{titleCase folder}}',
  component: {{name}},
};

export const {{name}}Story = () => {
  return <{{name}} />;
};
{{name}}Story.story = { name: '{{name}}' };
