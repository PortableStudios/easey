import React from 'react';
import type { Story, Meta } from '@storybook/react';

import {{name}} from './{{name}}';

export default {
  title: '{{titleCase folder}}',
  component: {{name}},
} as Meta;

type Args = React.ComponentProps<typeof {{name}}>;

export const {{name}}Story: Story<Args> = (args) => {
  return <{{name}} {...args} />;
};
{{name}}Story.storyName = '{{name}}';
{{name}}Story.args = {};
