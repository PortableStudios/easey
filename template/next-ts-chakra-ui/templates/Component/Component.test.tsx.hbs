import React from 'react';

import { render } from '@/utils/testing';

import {{name}} from './{{name}}';

describe('{{name}}', () => {
  test('renders', () => {
    const testId = 'test';
    const { getByTestId } = render(<{{name}} data-testid={testId} />);

    getByTestId(testId);
  });
});
