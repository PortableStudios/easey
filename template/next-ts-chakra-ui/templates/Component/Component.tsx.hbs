import React from 'react';
import { Flex } from '@chakra-ui/core';
import type { FlexProps } from '@chakra-ui/core';

type Props = FlexProps;

const {{name}}: React.FC<Props> = ({ children, ...rest }) => {
  return <Flex {...rest}>{children}</Flex>;
};

export default {{name}};
