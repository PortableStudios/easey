/**
 * This file should import any CSS styles which are needed for the entire app.
 * This allows us to import the same CSS in SvelteKit and Storybook without duplicating the code.
 */
import '@fontsource-variable/inter';
import '@fontsource-variable/roboto-mono';
import './app.pcss';
